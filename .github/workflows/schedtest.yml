name: schedtest
# TEMPORARY: proves whether GitHub fires scheduled events for this repo at all.
# Delete once we have an answer.
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scheduled fire at $(date -u)"
